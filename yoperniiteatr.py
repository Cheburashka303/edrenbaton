"""Функция кодирования в код Морзе будет называться encode_to_morse(text).

Функция декодирования называется decode_from_morse(code)."""

def encode_to_morse(text: str) -> str:
    pass


def decode_from_morse(code: str) -> str:
    pass
